<UserControl
    x:Class="App3.PlayerPopUp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
   
    <Grid Background="#7F000000">
        <MediaElement x:Name="mediaElement" AreTransportControlsEnabled="True"  AudioCategory="Media"  Margin="20" />
        <Button x:Name="Close" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top"  Background="#7F000000" Click="Close_Click">
            <StackPanel>
                <SymbolIcon Symbol="Cancel" />
            </StackPanel>
        </Button>
       
    </Grid>
</UserControl>
